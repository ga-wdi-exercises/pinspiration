<h2>All Boards</h2>
<% @boards.each do |board| %>
<div class='board'>
  <%= link_to board.title, board_path(board) %>
</div>
<% end %>

<% if current_user %>
<h2>My Boards</h2>
  <% current_user.boards.each do |board| %>
    <%= link_to board.title, board_path(board) %>
  <% end %>
<% end %>